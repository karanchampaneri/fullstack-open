```mermaid
sequenceDiagram
    participant browser
    participant server
    
    Note over browser: User writes note and clicks Save
    Note over browser: spa.js prevents default form submit
    Note over browser: Create new note locally, add it to notes list, and update DOM
    browser->>server: POST /exampleapp/new_note_spa as JSON data
    Note over server: Process and store new note
    server-->>browser: 201 Created
```
